// 프라임 스마트 아이 안과(가상 브랜드) 샘플 — 진료 항목·의료진 이름의 단일 출처.
//
// 왜 한 곳에 모으나: 적합도 계산기·인공수정체 시뮬레이터·의료진 카드가 각자 문자열을 만들어
// 예약 폼에 넘기고 있었다. 폼은 그 값을 `includes(앞 5글자)` 로 헐겁게 맞춰 보고 있어서,
// 한 글자만 어긋나도 「고른 것과 다른 항목이 예약 요약에 찍히는」 조용한 어긋남이 났다.
// (실제로 계산기의 고도난시 분기는 화면에 「렌티큘 추출술」을 띄우고 폼에는 「라식」을 넘겼다.)
//
// 시술·장비 이름은 전부 **일반 명칭**이다. 특정 제조사가 상표로 가진 레이저 장비명·시술명·진단기기명·
// 정렬 기술명·인공수정체 브랜드는 쓰지 않는다. 그런 이름을 가상 병원에 붙이면, 이 템플릿을
// 그대로 받아 쓰는 실제 의료기관이 「보유하지 않은 장비·자격」을 광고하게 된다.

/** 예약 폼의 진료·수술 항목 — 다른 구역에서 넘기는 값도 반드시 이 중 하나여야 한다. */
export const SERVICES = {
  klex: '렌티큘 추출술 (KLEx)',
  topoLasik: '토포가이드 맞춤 라식',
  phakicIol: '안내렌즈삽입술 (유수정체 인공수정체)',
  cataract: '노안 · 프리미엄 백내장',
  exam: '50단계 정밀 안종합 검진',
  revision: '재수술(2차 교정) 상담',
} as const;

export const SERVICE_LIST: readonly string[] = Object.values(SERVICES);

/** 예약 폼의 의료진 — 의료진 카드의 「지정 진료 예약」도 이 값을 넘긴다. */
export const DOCTORS = {
  any: '상관없음 (가장 빠른 일정 우선)',
  kang: '강현우 대표원장 (시력교정)',
  yoon: '윤소희 대표원장 (노안 · 백내장 · 망막)',
} as const;

export const DOCTOR_LIST: readonly string[] = Object.values(DOCTORS);

/** 시술 효과를 보여 주는 화면(시뮬레이터·비교표)에 함께 붙이는 고지. 의료법 제56조 제2항 대응. */
export const OUTCOME_DISCLAIMER =
  '예시 화면입니다. 시술 결과에는 개인차가 있으며, 모든 수술에는 부작용이 따를 수 있습니다. 적응증과 주의사항은 반드시 진료 시 의료진에게 확인하세요.';

/** 샘플 폼 제출 전 고지 — 개인정보를 받는 자리마다 눈에 띄게 둔다. */
export const SAMPLE_FORM_NOTICE =
  '샘플 사이트입니다 — 입력하신 내용은 어디에도 전송되지 않습니다.';
